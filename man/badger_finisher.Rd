% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/badger_finisher.R
\name{badger_finisher}
\alias{badger_finisher}
\title{Save a publication-ready Badger graphic}
\usage{
badger_finisher(
  plot,
  head = "Insert Headline",
  source = "Insert Source",
  logo_ref = system.file("img", "Badger-Institute-Icon.png", package = "badgerstyle"),
  filename = "plot.png",
  aspect = c("default", "1col", "2col", "web", "ppt", "custom"),
  border = TRUE,
  height = 5,
  width = 9.55,
  register_fonts = TRUE,
  title_family = "Franklin Gothic Demi Cond",
  text_family = "Franklin Gothic Medium Cond",
  title_lineheight = 1.2,
  source_lineheight = 1.2,
  title_plot_padding = 8,
  title_border_padding = 4
)
}
\arguments{
\item{plot}{A ggplot object to render.}

\item{head}{Character string used as the headline above the plot.}

\item{source}{Character string used as the source line below the plot.}

\item{logo_ref}{Path to the PNG logo. Defaults to the Badger Institute icon
installed with the package.}

\item{filename}{Output path for the PNG file.}

\item{aspect}{One of `"default"`, `"1col"`, `"2col"`, `"web"`, `"ppt"`,
or `"custom"`. The preset determines the output dimensions in inches.}

\item{border}{Logical; draw a gray border around the finished graphic.}

\item{height, width}{Custom output dimensions in inches. Used only when
`aspect = "custom"`.}

\item{register_fonts}{Logical; register the Badger fonts before rendering.
Registration is cached for the remainder of the R session.}

\item{title_family, text_family}{Font families used for the headline and
source line. The defaults use the standard Badger Institute fonts.}

\item{title_lineheight, source_lineheight}{Positive numeric line-height
multipliers for multiline headlines and source lines. Smaller values
tighten the spacing between lines. Both default to `1.2`, matching
grid's standard text spacing.}

\item{title_plot_padding, title_border_padding}{Non-negative spacing in
points below and above the headline, respectively. These independently
control the headline-to-plot gap and the top-border-to-headline inset.
For backward compatibility, calls that omit all four line-height and
padding arguments use the original Badger finisher layout exactly.}
}
\value{
Invisibly returns the result of closing the PNG graphics device.
}
\description{
Renders a ggplot to a high-resolution PNG with a separate headline, source
line, Badger Institute icon, and optional border. The output dimensions can
use a publication preset or custom dimensions.
}
\examples{
\dontrun{
library(ggplot2)
p <- ggplot(mtcars, aes(wt, mpg)) + geom_point() + badger_style()
badger_finisher(
  p,
  head = "Fuel economy falls as vehicle weight rises",
  source = "Source: Motor Trend",
  filename = "fuel-economy.png",
  aspect = "web"
)
}

}
\seealso{
[badger_style()], [badger_publish()]
}
