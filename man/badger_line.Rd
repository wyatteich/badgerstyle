% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/badger_line.R
\name{badger_line}
\alias{badger_line}
\title{Add styled lines with white backdrop to a ggplot}
\usage{
badger_line(plot = NULL, df, lw = 2.5, group_var, x_var, y_var, by = NULL)
}
\arguments{
\item{plot}{An optional ggplot object. Used to inherit \code{df},
\code{group_var}, \code{x_var}, and/or \code{y_var} from the plot's
\code{data} and \code{mapping}. Usually this can be omitted: when the
result is added with \code{+}, it inherits from the plot on its left.}

\item{df}{A data frame. Defaults to \code{plot$data} if not supplied.}

\item{lw}{Numeric. Base line width. Backdrop is drawn at \code{lw * 1.45}.
Defaults to \code{2.5}.}

\item{group_var}{Unquoted or quoted variable name for the grouping aesthetic
(typically mapped to \code{color}). Defaults to the \code{colour} mapping
of \code{plot}.}

\item{x_var}{Unquoted or quoted variable name for the x-axis. Defaults to
the \code{x} mapping of \code{plot}.}

\item{y_var}{Unquoted or quoted variable name for the y-axis. Defaults to
the \code{y} mapping of \code{plot}. Used to drop NA rows before locating
each group's first and last observed data point.}

\item{by}{Optional character vector of facet columns. When omitted in the
deferred form, simple `facet_wrap()` and `facet_grid()` variables are
inferred from the plot so endpoints are computed within each panel.}
}
\value{
When plot information must be inherited, a deferred component that
  ggplot resolves when it is added with \code{+}. Otherwise, a list of
  ggplot layer objects addable to a ggplot via \code{+}.
}
\description{
Adds four layers for each series, in visual-object order: its white backdrop
line, white endpoint masks, colored line, and colored endpoints. Completing
one series before drawing the next preserves intentional white separation at
crossings and prevents another series' endpoints from being interleaved
with the wrong line.
}
\details{
Endpoints are computed per group on rows where \code{y_var} is non-missing,
so series that start late, end early, or have internal NAs still receive
terminal points at their own first and last observed data point.
}
\examples{
library(ggplot2)

series <- data.frame(
  year = rep(2020:2024, 2),
  value = c(1:5, 2:6),
  place = rep(c("WI", "MN"), each = 5)
)

# Inherit data and mappings from the plot on the left.
ggplot(series, aes(year, value, colour = place)) +
  badger_line(lw = 1.4)

# The original API remains supported.
p <- ggplot(series, aes(year, value, colour = place))
p + badger_line(p, lw = 1.4)

}
\seealso{
\code{\link{find_endpoints}}
}
